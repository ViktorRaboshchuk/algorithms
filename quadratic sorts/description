Количество операций которое трубуется на сортировку массива = O(N^2)

- insert sort(сортировка вставками)
происходит сортировка под массива к которому постепенно добавляются переменные
- choice sort(сортировка методом выбора)
происходит нахождение поиск min(max) первого ЭМ(элемента массива), после его нахождение последующие поиски второго ЭМ не будут включать в себя первый ЭМ,
при поиске 3-го ЭМ в поиске не будут включены первый и второй ЭМ. Последни ЭМ сортировать не нужно.
- bubble sort(сортировка пузырьком)
после первой итерации min(max) значение будет в конце. после второй итерации предпоследний ЭМ будет на своем месте, и т.д.


